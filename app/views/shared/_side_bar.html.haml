.side_bar
  .side__header
    .top-items
      %p.top-items__user__name
        = current_user.name
      %ul.top-items__top-item
        %li.label{class: "top-items__top-item"}
          = link_to new_group_path do
            = icon('fas', 'edit')
        %span.mgr-5
        %label{class: "top-items__top-item"}
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'icon')
  .groups
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません。